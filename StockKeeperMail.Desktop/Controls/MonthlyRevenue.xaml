<UserControl x:Class="StockKeeperMail.Desktop.Controls.MonthlyRevenue"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockKeeperMail.Desktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"
                   Content="Статистика заказов"
                   HorizontalAlignment="Center"
                   FontSize="24"
                   FontWeight="Bold"/>

            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding OrderChartPeriods}"
                      SelectedItem="{Binding SelectedOrderChartPeriod, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource MaterialDesignComboBox}"
                      Padding="8"
                      VerticalAlignment="Center"/>
        </Grid>

        <lvc:CartesianChart Series="{Binding MonthlySales}" Grid.Row="1" LegendLocation="Right">

            <lvc:CartesianChart.DataTooltip>
                <lvc:DefaultTooltip SelectionMode="{x:Null}" IsWrapped="False" />
            </lvc:CartesianChart.DataTooltip>

            <lvc:CartesianChart.AxisX>
                <lvc:Axis Labels="{Binding MonthlySalesXLabel}">
                    <lvc:Axis.Separator>
                        <lvc:Separator IsEnabled="False" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>

            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Количество заказов"
                          MinValue="0"
                          LabelFormatter="{Binding MonthlySalesFormatter}">
                    <lvc:Axis.Separator>
                        <lvc:Separator IsEnabled="True" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>

        </lvc:CartesianChart>
    </Grid>
</UserControl>