<UserControl x:Class="StockKeeperMail.Desktop.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockKeeperMail.Desktop.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" >
    <Grid>
        <ScrollViewer Style="{StaticResource MaterialDesignScrollViewer}" VerticalScrollBarVisibility="Auto" Margin="0,0,0,0" Background="{DynamicResource MaterialDesignPaper}" >
            <StackPanel>
                <Grid Margin="16 20 16 0">
                    <Border Padding="12" CornerRadius="16" BorderBrush="{DynamicResource BrandPrimaryBrush}" BorderThickness="1" Background="{DynamicResource MaterialDesignPaper}">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="pack://application:,,,/Assets/IMSLogo.png" Width="42" Height="42" VerticalAlignment="Center"/>
                            <StackPanel Margin="10 0 0 0" VerticalAlignment="Center">
                                <TextBlock FontSize="16" FontWeight="Bold">
                                    <Run Text="ВОЛГО" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                    <Run Text="-МЕД" Foreground="{DynamicResource BrandAccentBrush}"/>
                                </TextBlock>
                                <TextBlock Text="Склад и почта" FontSize="10" Opacity="0.75"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid HorizontalAlignment="Center" Margin="0 10 0 10">
                    <Label FontSize="16" VerticalAlignment="Center" FontWeight="Bold" Padding="20 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Здравствуйте, " />
                            <TextBlock Text="{Binding CurrentStaff.StaffUsername}"/>
                        </StackPanel>
                    </Label>
                </Grid>


                <Grid Margin="0 20 0 0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToDashboardCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="ViewDashboardOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Панель управления" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToOrderListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.OrdersView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="CartCheck" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Заказы" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToCustomerListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.CustomersView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="BusinessCardOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Клиенты" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>


                <Grid Margin="0 20 0 0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToProductListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.ProductsView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="BoxOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Товары" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToStorageListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.StoragesView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="Storage" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Склады" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToDefectiveListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.DefectivesView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="GlassBroken" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Брак" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>




                <Grid Margin="0 20 0 0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToCategoryListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.CategoriesView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="CategoryOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Категории" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToLocationListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.LocationsView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="LocationOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Склады/Магазины" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToSupplierListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.SuppliersView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="ClipboardListOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Поставщики" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>


                <Grid Margin="0 20 0 0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToRoleListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.RolesView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="PersonOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Роли" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding NavigateToStaffListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.StaffsView}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                                <materialDesign:PackIcon Kind="CustomerService" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                                <Label Margin="5 0 0 0" Content="Сотрудники" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>


                <Grid Margin="0 20 0 0">
                    <Button Command="{Binding NavigateToMessageListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                        <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                            <materialDesign:PackIcon Kind="EmailOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                            <Label Margin="5 0 0 0" Content="Сообщения" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Grid Margin="0 20 0 0">
                    <Button Command="{Binding NavigateToLogListCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CurrentStaff.Role.LogsView}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                            <materialDesign:PackIcon Kind="File" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                            <Label Margin="5 0 0 0" Content="Журнал активности" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </Grid>


                <Grid Margin="0 20 0 0">
                    <Button Command="{Binding LogOutCommand}" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="Transparent">
                        <StackPanel Orientation="Horizontal" Margin="20 0 0 0">
                            <materialDesign:PackIcon Kind="Logout" VerticalAlignment="Center" Foreground="{DynamicResource BrandPrimaryBrush}"/>
                            <Label Margin="5 0 0 0" Content="Выход" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>

        </ScrollViewer>
    </Grid>
</UserControl>