<UserControl x:Class="StockKeeperMail.Desktop.Views.CreateReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="900">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Создание отчета"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   FontWeight="Bold"/>

        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="Название отчета" Margin="0,0,0,8" FontWeight="SemiBold"/>
            <TextBox Text="{Binding ReportName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Например: Отчет_за_апрель"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,18,0,0">
            <TextBlock Text="Выберите разделы, которые должны войти в отчет" FontWeight="SemiBold" Margin="0,0,0,12"/>
            <ItemsControl ItemsSource="{Binding SectionOptions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,10,10"
                                Padding="12,8"
                                CornerRadius="10"
                                BorderBrush="{DynamicResource BrandPrimaryBrush}"
                                BorderThickness="1">
                            <CheckBox Content="{Binding DisplayName}"
                                      IsChecked="{Binding IsSelected}"
                                      IsEnabled="{Binding IsAvailable}"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Margin="0,18,0,0"
                    Padding="12"
                    CornerRadius="8"
                    Background="{DynamicResource BrandSurfaceBrush}">
                <TextBlock Text="Сохранение выполняется в Excel XML (*.xml), который можно открыть в Microsoft Excel. Для печати можно использовать кнопку ниже."
                           TextWrapping="Wrap"/>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,22,0,0">
            <Button Margin="0,0,10,0"
                    Command="{Binding PrintReportCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignPaperButton}"/>
                </Button.Style>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Printer" Margin="0,0,6,0"/>
                    <TextBlock Text="Печать"/>
                </StackPanel>
            </Button>

            <Button Margin="0,0,10,0"
                    Command="{Binding CloseDialogCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignPaperButton}"/>
                </Button.Style>
                <TextBlock Text="Закрыть"/>
            </Button>

            <Button Command="{Binding CreateReportCommand}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                        <Setter Property="Background" Value="{DynamicResource BrandPrimaryBrush}"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </Button.Style>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,6,0"/>
                    <TextBlock Text="Сохранить в файл"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
