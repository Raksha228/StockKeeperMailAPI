<UserControl x:Class="StockKeeperMail.Desktop.Controls.CurrentMonthRevenue"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Width="44" Height="44" CornerRadius="14" Background="{DynamicResource BrandSubtleSurfaceBrush}" VerticalAlignment="Center">
            <materialDesign:PackIcon Kind="CashMultiple" Width="22" Height="22" Foreground="{DynamicResource BrandPrimaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
            <TextBlock Text="Выручка" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource BrandPrimaryBrush}"/>
            <TextBlock Text="За этот месяц" FontSize="10" Foreground="{DynamicResource BrandMutedTextBrush}"/>
            <TextBlock Text="{Binding CurrentMonthRevenue}" Margin="0,6,0,0" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource BrandTextBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
