<UserControl x:Class="StockKeeperMail.Desktop.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="NavTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource BrandNavTextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="12,3"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BrandNavTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="16" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#24FFFFFF"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#3DFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <ScrollViewer Style="{StaticResource MaterialDesignScrollViewer}" VerticalScrollBarVisibility="Auto" Background="Transparent">
            <StackPanel>
                <Grid Margin="16,20,16,0">
                    <Border Padding="12" CornerRadius="16" BorderBrush="#33FFFFFF" BorderThickness="1" Background="Transparent">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="pack://application:,,,/Assets/IMSLogo.png" Width="42" Height="42" VerticalAlignment="Center"/>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock FontSize="16" FontWeight="Bold">
                                    <Run Text="ВОЛГО" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                    <Run Text="-МЕД" Foreground="{DynamicResource BrandAccentBrush}"/>
                                </TextBlock>
                                <TextBlock Text="Склад и почта" FontSize="10" Foreground="White" Opacity="0.85"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid HorizontalAlignment="Center" Margin="0,10,0,10">
                    <Border Background="#1EFFFFFF" CornerRadius="16" Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Здравствуйте, " FontSize="16" FontWeight="Bold" Foreground="{DynamicResource BrandNavTextBrush}"/>
                            <TextBlock Text="{Binding CurrentStaff.StaffUsername}" FontSize="16" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToDashboardCommand}">
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="ViewDashboardOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Панель управления"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToOrderListCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.OrdersView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="CartCheck" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Заказы"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToCustomerListCommand}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStaff.Role.CustomersView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="BusinessCardOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Клиенты"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToProductListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.ProductsView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="BoxOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Товары"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToStorageListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.StoragesView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="Database" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Склады"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToDefectiveListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.DefectivesView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="GlassBroken" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Брак"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToCategoryListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.CategoriesView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="ShapeOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Категории"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToLocationListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.LocationsView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="MapMarkerOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Склады/Магазины"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToSupplierListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.SuppliersView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="ClipboardListOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Поставщики"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <StackPanel>
                        <Button Command="{Binding NavigateToRoleListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.RolesView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="AccountOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Роли"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToStaffListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.StaffsView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="AccountGroupOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Сотрудники"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding NavigateToCredentialListCommand}">
                            <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.RolesAdd}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                            <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                                <materialDesign:PackIcon Kind="BadgeAccountOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                                <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Учетные данные"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <Button Command="{Binding NavigateToMessageListCommand}">
                        <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                            <materialDesign:PackIcon Kind="EmailOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                            <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Сообщения"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <Button Command="{Binding NavigateToLogListCommand}">
                        <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><DataTrigger Binding="{Binding CurrentStaff.Role.LogsView}" Value="False"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Button.Style>
                        <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                            <materialDesign:PackIcon Kind="FileDocumentOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                            <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Журнал активности"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Grid Margin="0,20,0,0">
                    <Button Command="{Binding NavigateToReportListCommand}">
                        <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                            <materialDesign:PackIcon Kind="ChartBoxOutline" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                            <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Отчеты"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Grid Margin="0,20,0,20">
                    <Button Command="{Binding LogOutCommand}">
                        <StackPanel Orientation="Horizontal" Margin="2,0,0,0">
                            <materialDesign:PackIcon Kind="Logout" VerticalAlignment="Center" Foreground="{DynamicResource BrandAccentLightBrush}"/>
                            <TextBlock Margin="8,0,0,0" Style="{StaticResource NavTextStyle}" Text="Выход"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
