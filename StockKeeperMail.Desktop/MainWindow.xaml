<Window x:Class="StockKeeperMail.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockKeeperMail.Desktop"
        xmlns:controls="clr-namespace:StockKeeperMail.Desktop.Controls"
        mc:Ignorable="d"
        d:Width="1920"
        d:Height="1080"
        Title="Волго-Мед — корпоративная система"
        WindowState="Maximized"
        Icon="pack://application:,,,/Assets/logo.png"
        WindowStyle="None"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource BrandTextBrush}"
        FontFamily="Segoe UI">

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="256" Margin="16,16,0,16" CornerRadius="28" Background="{DynamicResource BrandSidebarGradientBrush}" ClipToBounds="True">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.20" Color="#312E81"/>
                </Border.Effect>
                <controls:NavigationBar/>
            </Border>

            <Border Grid.Column="1" Margin="16" CornerRadius="32" Background="{DynamicResource BrandWindowShellBrush}" ClipToBounds="True">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.12" Color="#334155"/>
                </Border.Effect>
                <Grid Background="{DynamicResource BrandWindowInnerSurfaceBrush}">
                    <ContentControl Content="{Binding CurrentViewModel}"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10" Padding="18,7" CornerRadius="18" Background="{DynamicResource BrandFooterBrush}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ИП Бахолдина • Волго-Мед" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource BrandMutedTextBrush}"/>
                <TextBlock Text="  •  " FontSize="11" Foreground="{DynamicResource BrandMutedTextBrush}"/>
                <TextBlock Text="Инвентаризация складских запасов и внутренняя почта" FontSize="11" Foreground="{DynamicResource BrandMutedTextBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
