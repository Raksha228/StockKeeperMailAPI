<UserControl x:Class="StockKeeperMail.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StockKeeperMail.Desktop.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:StockKeeperMail.Desktop.Converters"
             mc:Ignorable="d">

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Ellipse Width="420" Height="420" Fill="#334F46E5" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-120,-160,0,0"/>
        <Ellipse Width="360" Height="360" Fill="#3306B6D4" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-110,-130"/>

        <materialDesign:Card Width="900" Height="560" UniformCornerRadius="32" Background="{DynamicResource BrandCardBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" materialDesign:ElevationAssist.Elevation="Dp12">
            <materialDesign:DialogHost CloseOnClickAway="True" x:Name="DialogHost">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*"/>
                        <ColumnDefinition Width="0.95*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" CornerRadius="32,0,0,32" Background="{DynamicResource BrandHeroGradientBrush}" Padding="42" ClipToBounds="True">
                        <Grid>
                            <Ellipse Width="260" Height="260" Fill="#22FFFFFF" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-110,-120,0"/>
                            <Ellipse Width="190" Height="190" Fill="#1AFFFFFF" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-70,0,0,-80"/>
                            <StackPanel VerticalAlignment="Center">
                                <Border Width="92" Height="92" CornerRadius="26" Background="#F2FFFFFF" HorizontalAlignment="Left" Padding="10">
                                    <Image Source="pack://application:,,,/Assets/IMSLogo.png" Stretch="Uniform"/>
                                </Border>

                                <TextBlock Margin="0,26,0,0" FontSize="42" FontWeight="Black" Foreground="White">
                                    <Run Text="ВОЛГО"/>
                                    <Run Text="-МЕД"/>
                                </TextBlock>
                                <TextBlock Margin="0,12,0,0" Text="Корпоративная система складского учета" FontSize="18" FontWeight="SemiBold" Foreground="#ECFEFF"/>
                                <TextBlock Margin="0,8,30,0" Text="Управление товарами, заказами, поставщиками, отчетами и внутренней почтой в одном рабочем пространстве." TextWrapping="Wrap" FontSize="14" LineHeight="22" Foreground="#CCFFFFFF"/>

                                <StackPanel Orientation="Horizontal" Margin="0,34,0,0">
                                    <Border CornerRadius="18" Background="#26FFFFFF" Padding="14,8" Margin="0,0,10,0">
                                        <TextBlock Text="Склад" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border CornerRadius="18" Background="#26FFFFFF" Padding="14,8" Margin="0,0,10,0">
                                        <TextBlock Text="Почта" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border CornerRadius="18" Background="#26FFFFFF" Padding="14,8">
                                        <TextBlock Text="Отчеты" Foreground="White" FontWeight="SemiBold"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Column="1" Margin="48,30,48,36">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <materialDesign:PopupBox HorizontalAlignment="Right" PlacementMode="BottomAndAlignRightEdges" StaysOpen="False" Height="32" Width="32">
                            <StackPanel MinWidth="190">
                                <StackPanel Margin="16,10,8,6" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock VerticalAlignment="Center" Text="Тёмная тема"/>
                                    <ToggleButton Cursor="Hand" ToolTip="Включить тёмную тему" Margin="12,0,8,0" x:Name="themeToggle" IsChecked="{Binding IsDarkTheme}" Command="{Binding ToggleThemeCommand}"/>
                                </StackPanel>
                                <Button ToolTip="Проблемы со входом?" Margin="8,8,8,0" Content="Помощь" Command="{Binding HelpCommand}"/>
                                <Button x:Name="btn_exit" ToolTip="Закрыть приложение" Margin="8,4,8,8" Content="Выход" Command="{Binding ExitAppCommand}"/>
                            </StackPanel>
                        </materialDesign:PopupBox>

                        <StackPanel Grid.Row="1" VerticalAlignment="Center">
                            <TextBlock Text="Добро пожаловать" FontSize="30" FontWeight="Bold" Foreground="{DynamicResource BrandTextBrush}"/>
                            <TextBlock Text="Введите учетные данные сотрудника для продолжения работы" Margin="0,8,0,28" FontSize="14" Foreground="{DynamicResource BrandMutedTextBrush}" TextWrapping="Wrap"/>

                            <TextBox x:Name="txtUsername"
                                     materialDesign:HintAssist.Hint="Имя пользователя"
                                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                            <PasswordBox x:Name="txtPassword"
                                         materialDesign:HintAssist.Hint="Пароль"/>

                            <Button x:Name="loginBtn"
                                    Height="48"
                                    Margin="0,12,0,0"
                                    Background="{DynamicResource BrandPrimaryBrush}"
                                    Foreground="White"
                                    BorderBrush="{DynamicResource BrandPrimaryBrush}"
                                    materialDesign:ButtonAssist.CornerRadius="16"
                                    materialDesign:ElevationAssist.Elevation="Dp4"
                                    Command="{Binding LoginUserCommand}"
                                    CommandParameter="{Binding ElementName=txtPassword}">
                                <TextBlock Text="Войти в систему" Foreground="White" FontWeight="SemiBold"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </materialDesign:DialogHost>
        </materialDesign:Card>
    </Grid>
</UserControl>
