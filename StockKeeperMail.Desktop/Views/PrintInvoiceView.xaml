<UserControl x:Class="StockKeeperMail.Desktop.Views.PrintInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StockKeeperMail.Desktop.Views"
             xmlns:controls="clr-namespace:StockKeeperMail.Desktop.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" >
    <Grid Background="{DynamicResource MaterialDesignBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="20,20,20,0"
                Padding="12"
                CornerRadius="8"
                Background="{DynamicResource MaterialDesignPaper}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1">
            <DockPanel LastChildFill="False">
                <Button Width="55"
                        Height="50"
                        Command="{Binding PrintCommand}"
                        CommandParameter="{Binding ElementName=InvoiceDocument}">
                    <materialDesign:PackIcon Kind="Printer" Width="25" Height="25"/>
                </Button>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Border Background="White"
                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                    BorderThickness="1"
                    CornerRadius="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <controls:InvoiceDocumentControl x:Name="InvoiceDocument"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
