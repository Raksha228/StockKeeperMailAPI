<UserControl x:Class="StockKeeperMail.Desktop.Views.DashboardView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:StockKeeperMail.Desktop.Controls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Margin="34">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="30" Padding="28" Background="{DynamicResource BrandHeroGradientBrush}" Margin="0,0,0,22">
            <Border.Effect>
                <DropShadowEffect BlurRadius="22" ShadowDepth="0" Opacity="0.18" Color="#4F46E5"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Панель управления" FontSize="30" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Ключевые показатели склада, заказов и продаж" Margin="0,6,0,0" FontSize="14" Foreground="#DFFBFF"/>
                </StackPanel>
                <materialDesign:Card Grid.Column="1" UniformCornerRadius="22" Margin="12,0,0,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp2">
                    <controls:CurrentMonthRevenue/>
                </materialDesign:Card>
                <materialDesign:Card Grid.Column="2" UniformCornerRadius="22" Margin="12,0,0,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp2">
                    <controls:CurrentMonthOrders/>
                </materialDesign:Card>
                <materialDesign:Card Grid.Column="3" UniformCornerRadius="22" Margin="12,0,0,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp2">
                    <controls:ProductsInStockControl/>
                </materialDesign:Card>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="26" Background="{DynamicResource BrandSubtleSurfaceBrush}" Padding="22" Margin="0,0,14,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Доставка" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource BrandTextBrush}"/>
                    <TextBlock Text="Статусы текущих заказов" FontSize="13" Foreground="{DynamicResource BrandMutedTextBrush}" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <materialDesign:Card Grid.Column="1" UniformCornerRadius="24" Margin="0,0,14,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp3">
                <controls:ProcessingOrderCountControl/>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="2" UniformCornerRadius="24" Margin="0,0,14,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp3">
                <controls:ShippedOrderCountControl/>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="3" UniformCornerRadius="24" Margin="0,0,14,0" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp3">
                <controls:InTransitOrderCountControl/>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="4" UniformCornerRadius="24" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp3">
                <controls:DeliveredOrderCountControl/>
            </materialDesign:Card>
        </Grid>

        <materialDesign:Card Grid.Row="2" UniformCornerRadius="28" Background="{DynamicResource BrandCardBrush}" materialDesign:ElevationAssist.Elevation="Dp3">
            <Grid Margin="18">
                <controls:MonthlyRevenue/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
