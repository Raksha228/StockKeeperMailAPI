<UserControl x:Class="StockKeeperMail.Desktop.Controls.CustomHeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:StockKeeperMail.Desktop.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Border Background="{DynamicResource BrandFooterBrush}" CornerRadius="22" Padding="14,10" BorderBrush="{DynamicResource BrandBorderBrush}" BorderThickness="1">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <materialDesign:PackIcon Kind="AccountCircleOutline" Width="24" Height="24" Foreground="{DynamicResource BrandPrimaryBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Имя пользователя" Margin="10,0,16,0" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource BrandTextBrush}"/>
            <Button Content="Выход" FontSize="13" Command="{}" Style="{StaticResource MaterialDesignOutlinedButton}" materialDesign:ButtonAssist.CornerRadius="14"/>
        </StackPanel>
    </Border>
</UserControl>
